# Legends Never Die 综合测试用例套件

> 基于 PRD v1.0 与 Web 版 TypeScript/Phaser 3 实现，覆盖 8 大质量维度。

---

## 一、操作正确性 (Operational Correctness)

### 1.1 键盘输入 — 跳跃

| 编号 | TC-OP-001 |
|---|---|
| 前置条件 | 游戏处于 PLAYING 状态，角色在地面上，未跳跃、未飞行 |
| 执行步骤 | 1. 按下 `Space` 键 |
| 预期结果 | 角色执行一次跳跃；播放 `jump.wav`；`hero.isJumping` 变为 `true` |
| 通过标准 | 角色离地上升后自然回落至 `GROUND_Y - 96`；跳跃期间 `velocityY` 初始值 = `JUMP_VELOCITY * jumpVelocityModifier` |
| 失败标准 | 角色未离地 / 无音效 / 跳跃后未回到地面 / 连按 Space 出现二段跳 |

### 1.2 键盘输入 — 跳跃（空中/飞行状态锁定）

| 编号 | TC-OP-002 |
|---|---|
| 前置条件 | 角色正在跳跃中（`isJumping=true`）或飞行中（`isFlying=true`） |
| 执行步骤 | 1. 按下 `Space` 键 |
| 预期结果 | 无任何响应，不触发二次跳跃 |
| 通过标准 | `hero.isJumping` 保持不变；角色 Y 坐标无额外变化 |
| 失败标准 | 角色再次跳跃 / 出现浮空 |

### 1.3 键盘输入 — 射击

| 编号 | TC-OP-003 |
|---|---|
| 前置条件 | 游戏处于 PLAYING 状态；射击冷却已结束（`nowMs - lastShootAt >= shootCooldownMs`） |
| 执行步骤 | 1. 按下 `K` 键 |
| 预期结果 | 发射 `fireLevel` 颗子弹（上限 5）；播放 `shoot.wav`；更新 `lastShootAt` |
| 通过标准 | 子弹数量 = `min(fireLevel, 5)`；每颗子弹 `damage = 1 + bulletDamageBonus`；子弹向右飞行（速度 520px/s） |
| 失败标准 | 子弹数量错误 / 无音效 / 冷却未生效可连射 |

### 1.4 键盘输入 — 射击冷却

| 编号 | TC-OP-004 |
|---|---|
| 前置条件 | 刚完成一次射击，`nowMs - lastShootAt < shootCooldownMs` |
| 执行步骤 | 1. 立即再次按下 `K` 键 |
| 预期结果 | 不发射子弹 |
| 通过标准 | 无新子弹生成；`lastShootAt` 不更新 |
| 失败标准 | 冷却期内仍可射击 |

### 1.5 键盘输入 — 导弹发射

| 编号 | TC-OP-005 |
|---|---|
| 前置条件 | `missileCount > 0`；导弹冷却已结束（`nowMs - lastMissileAt >= 1500ms`） |
| 执行步骤 | 1. 按下 `L` 键 |
| 预期结果 | 发射 1 枚导弹；`missileCount -= 1`；播放 `missile.wav`；更新 `lastMissileAt` |
| 通过标准 | 导弹 `damage=2`，`penetrate=true`，`explosive=hasExplosiveMissiles`；HUD 导弹数减 1 |
| 失败标准 | 导弹数未扣减 / 冷却未生效 / 导弹数为 0 时仍可发射 |

### 1.6 键盘输入 — 导弹库存为 0

| 编号 | TC-OP-006 |
|---|---|
| 前置条件 | `missileCount === 0` |
| 执行步骤 | 1. 按下 `L` 键 |
| 预期结果 | 无导弹发射 |
| 通过标准 | 无新导弹生成；`missileCount` 保持 0 |
| 失败标准 | 导弹数变为负数 / 仍发射导弹 |

### 1.7 键盘输入 — 暂停/继续

| 编号 | TC-OP-007 |
|---|---|
| 前置条件 | 游戏处于 PLAYING 状态 |
| 执行步骤 | 1. 按下 `P` 或 `ESC` 键 |
| 预期结果 | 游戏暂停；`state = PAUSED`；弹出 PauseScene；BGM 暂停 |
| 通过标准 | 场景切换至 PauseScene；GameScene update 不再推进游戏逻辑 |
| 失败标准 | 游戏未暂停 / BGM 继续播放 / 无暂停界面 |

### 1.8 键盘输入 — 暂停后继续

| 编号 | TC-OP-008 |
|---|---|
| 前置条件 | 游戏处于 PAUSED 状态，PauseScene 已显示 |
| 执行步骤 | 1. 按下 `P` 或 `ESC` 键，或点击"继续"按钮 |
| 预期结果 | 游戏恢复；`state = PLAYING`；PauseScene 关闭；BGM 恢复 |
| 通过标准 | GameScene update 恢复执行；角色/敌人/子弹状态与暂停前一致 |
| 失败标准 | 游戏状态重置 / 敌人位置跳变 / BGM 从头播放 |

### 1.9 键盘输入 — 升级选卡

| 编号 | TC-OP-009 |
|---|---|
| 前置条件 | 升级选卡界面已弹出（LevelUpScene），显示 3 张卡片 |
| 执行步骤 | 1. 按下 `1` / `2` / `3` 键 |
| 预期结果 | 对应索引的升级卡被选中并立即生效 |
| 通过标准 | `hero` 对应属性按卡片效果变更；`pendingLevelUps -= 1`；选卡界面关闭或刷新 |
| 失败标准 | 按键无响应 / 选错卡片 / 属性未变更 |

### 1.10 触屏输入 — 跳跃/射击/导弹按钮

| 编号 | TC-OP-010 |
|---|---|
| 前置条件 | 移动端浏览器运行游戏；触屏按钮可见 |
| 执行步骤 | 1. 分别点击跳跃、射击、导弹按钮 |
| 预期结果 | 与键盘操作等效：跳跃按钮触发跳跃，射击按钮触发射击，导弹按钮触发导弹 |
| 通过标准 | 每个按钮功能与对应键盘按键一致 |
| 失败标准 | 按钮无响应 / 功能错位 / 多次点击穿透 |

### 1.11 触屏输入 — 升级选卡点击

| 编号 | TC-OP-011 |
|---|---|
| 前置条件 | 升级选卡界面已弹出 |
| 执行步骤 | 1. 点击任意一张卡片 |
| 预期结果 | 该卡片被选中并生效 |
| 通过标准 | 与键盘选卡结果一致 |
| 失败标准 | 点击无响应 / 点击区域偏移 / 误选 |

### 1.12 菜单界面 — 开始游戏

| 编号 | TC-OP-012 |
|---|---|
| 前置条件 | 游戏加载完成，MenuScene 显示 |
| 执行步骤 | 1. 按下 `Enter` 或 `Space`，或点击"开始游戏"按钮 |
| 预期结果 | 场景切换至 GameScene，游戏开始 |
| 通过标准 | 角色出现在 `HERO_X` 位置；BGM 播放；HUD 显示初始值 |
| 失败标准 | 场景未切换 / 角色未出现 / 无 BGM |

### 1.13 游戏结束 — 重新开始

| 编号 | TC-OP-013 |
|---|---|
| 前置条件 | GameOverScene 已显示 |
| 执行步骤 | 1. 按下 `Enter` 或 `Space`，或点击"再来一局"按钮 |
| 预期结果 | 所有游戏状态重置，重新开始一局 |
| 通过标准 | `hero` 属性恢复初始值；所有实体清除；距离/分数/击杀归零 |
| 失败标准 | 残留上一局实体 / 属性未重置 / 场景叠加 |

### 1.14 非法输入 — PLAYING 状态外操作

| 编号 | TC-OP-014 |
|---|---|
| 前置条件 | 游戏处于 PAUSED / LEVELUP / GAMEOVER 状态 |
| 执行步骤 | 1. 按下 `Space` / `K` / `L` |
| 预期结果 | 跳跃、射击、导弹操作均不响应 |
| 通过标准 | 角色无动作；无子弹/导弹生成 |
| 失败标准 | 暂停/选卡期间仍可操作角色 |

---

## 二、技能效果测试 (Skill Effects Testing)

### 2.1 火力升级 (fire-up)

| 编号 | TC-SK-001 |
|---|---|
| 前置条件 | `fireLevel < 5`；升级选卡界面出现"火力升级" |
| 执行步骤 | 1. 选择"火力升级"卡片 |
| 预期结果 | `fireLevel += 1`；下次射击发射子弹数增加 1 |
| 通过标准 | `fireLevel` 从 1→2→3→4→5 逐次递增；达到 5 后"火力升级"不再出现在候选池 |
| 失败标准 | `fireLevel` 超过 5 / 射击子弹数与 `fireLevel` 不匹配 / 达到上限后仍可抽取 |

### 2.2 射速升级 (shoot-speed)

| 编号 | TC-SK-002 |
|---|---|
| 前置条件 | `shootCooldownMs > 100`；升级选卡界面出现"射速升级" |
| 执行步骤 | 1. 选择"射速升级"卡片 |
| 预期结果 | `shootCooldownMs -= 20`（最低 100ms） |
| 通过标准 | 每次减少 20ms；达到 100ms 后不再出现；射击频率明显提升 |
| 失败标准 | 冷却时间低于 100ms / 射速无变化 / 超限后仍可抽取 |

### 2.3 子弹强化 (bullet-boost)

| 编号 | TC-SK-003 |
|---|---|
| 前置条件 | `bulletDamageBonus < 4`；升级选卡界面出现"子弹强化" |
| 执行步骤 | 1. 选择"子弹强化"卡片 |
| 预期结果 | `bulletDamageBonus += 1`；普通子弹伤害 `= 1 + bulletDamageBonus` |
| 通过标准 | 伤害从 2→3→4→5 递增；达到 +4 后不再出现 |
| 失败标准 | 伤害超过 5 / 伤害值不正确 / 超限后仍可抽取 |

### 2.4 导弹补给 (missile-supply)

| 编号 | TC-SK-004 |
|---|---|
| 前置条件 | 任意状态 |
| 执行步骤 | 1. 选择"导弹补给"卡片 |
| 预期结果 | `missileCount += 5` |
| 通过标准 | 导弹数正确增加 5；HUD 显示更新；无上限限制 |
| 失败标准 | 导弹数增加不正确 / HUD 未更新 |

### 2.5 爆裂弹头 (explosive-warhead)

| 编号 | TC-SK-005 |
|---|---|
| 前置条件 | `hasExplosiveMissiles === false` |
| 执行步骤 | 1. 选择"爆裂弹头"卡片；2. 发射导弹命中敌人 |
| 预期结果 | `hasExplosiveMissiles = true`；导弹命中时触发范围爆炸（半径 74px） |
| 通过标准 | 爆炸范围内所有敌人均受伤害；爆炸特效显示；选择后不再出现此卡 |
| 失败标准 | 无爆炸效果 / 爆炸范围伤害未生效 / 重复出现 |

### 2.6 生命提升 (life-up)

| 编号 | TC-SK-006 |
|---|---|
| 前置条件 | `maxLife < 8` |
| 执行步骤 | 1. 选择"生命提升"卡片 |
| 预期结果 | `maxLife += 1`；`life += 1` |
| 通过标准 | 生命上限从 3 逐步增至 8；当前生命同步增加；HUD 生命条更新 |
| 失败标准 | `maxLife` 超过 8 / `life` 超过 `maxLife` / 达到上限后仍可抽取 |

### 2.7 护甲装置 (armor-up)

| 编号 | TC-SK-007 |
|---|---|
| 前置条件 | `armor < 10` |
| 执行步骤 | 1. 选择"护甲装置"卡片 |
| 预期结果 | `armor += 2`（上限 10） |
| 通过标准 | 护甲值正确增加；护盾视觉特效显示；受击时先扣护甲；达到 10 后不再出现 |
| 失败标准 | 护甲超过 10 / 护盾特效未显示 / 受击未先扣护甲 |

### 2.8 紧急修复 (emergency-repair)

| 编号 | TC-SK-008 |
|---|---|
| 前置条件 | `life < maxLife` |
| 执行步骤 | 1. 选择"紧急修复"卡片 |
| 预期结果 | `life = min(life + 2, maxLife)` |
| 通过标准 | 生命恢复 2 点但不超过上限；满血时此卡不出现在候选池 |
| 失败标准 | 生命超过 `maxLife` / 满血时仍可抽取 |

### 2.9 韧性提升 (resilience)

| 编号 | TC-SK-009 |
|---|---|
| 前置条件 | `invulnerableMs < 800` |
| 执行步骤 | 1. 选择"韧性提升"卡片 |
| 预期结果 | `invulnerableMs += 100`（上限 800ms） |
| 通过标准 | 受击后无敌时间延长；达到 800ms 后不再出现 |
| 失败标准 | 无敌时间超过 800ms / 无敌时间未延长 / 超限后仍可抽取 |

### 2.10 轻量化 (lightweight)

| 编号 | TC-SK-010 |
|---|---|
| 前置条件 | `jumpScaleCount < 3` |
| 执行步骤 | 1. 选择"轻量化"卡片；2. 执行跳跃 |
| 预期结果 | `jumpScaleCount += 1`；`jumpVelocityModifier *= 1.15`；跳跃高度提升 15% |
| 通过标准 | 跳跃最大高度比基础值高约 15%/30%/45%（3 次叠加）；达到 3 次后不再出现 |
| 失败标准 | 跳跃高度无变化 / 叠加计算错误 / 超限后仍可抽取 |

### 2.11 飞行模块 (flight-module)

| 编号 | TC-SK-011 |
|---|---|
| 前置条件 | 任意状态 |
| 执行步骤 | 1. 选择"飞行模块"卡片 |
| 预期结果 | `isFlying = true`；角色移至 Y=180（中高空）；`flightRemainingMs = flightDurationMs`；显示飞行公告"Flight Online" |
| 通过标准 | 角色切换为飞行精灵 `heroFly`；飞行期间无需跳跃输入；无视地面障碍；倒计时结束后回到地面 |
| 失败标准 | 角色未升空 / 飞行期间仍受障碍碰撞 / 倒计时未结束就落地 / 落地后卡入障碍 |

### 2.12 飞行续航 (flight-endurance)

| 编号 | TC-SK-012 |
|---|---|
| 前置条件 | `flightDurationMs < 19000` |
| 执行步骤 | 1. 选择"飞行续航"卡片 |
| 预期结果 | `flightDurationMs += 3000`（上限 19000ms） |
| 通过标准 | 下次飞行持续时间增加 3 秒；达到 19s 后不再出现 |
| 失败标准 | 持续时间超过 19s / 持续时间未增加 / 超限后仍可抽取 |

### 2.13 起跳喷射 (jump-jet)

| 编号 | TC-SK-013 |
|---|---|
| 前置条件 | `jumpBoostCount < 3` |
| 执行步骤 | 1. 选择"起跳喷射"卡片；2. 执行跳跃 |
| 预期结果 | `jumpBoostCount += 1`；`jumpVelocityModifier *= 1.1`；起跳速度提升 10% |
| 通过标准 | 跳跃初速度增加；与轻量化叠加正确；达到 3 次后不再出现 |
| 失败标准 | 速度叠加计算错误 / 超限后仍可抽取 |

### 2.14 经验增幅 (exp-boost)

| 编号 | TC-SK-014 |
|---|---|
| 前置条件 | `expBoostCount < 3` |
| 执行步骤 | 1. 选择"经验增幅"卡片；2. 击杀敌人获取经验 |
| 预期结果 | `expBoostCount += 1`；经验倍率 = `1 + expBoostCount * 0.2` |
| 通过标准 | 经验获取量 = 基础值 × 倍率（1.2 / 1.4 / 1.6）；达到 3 次后不再出现 |
| 失败标准 | 经验倍率计算错误 / 超限后仍可抽取 |

### 2.15 幸运检索 (lucky-search)

| 编号 | TC-SK-015 |
|---|---|
| 前置条件 | `luckyCount < 2` |
| 执行步骤 | 1. 选择"幸运检索"卡片；2. 下次升级时观察卡片稀有度分布 |
| 预期结果 | `luckyCount += 1`；稀有卡权重 +10，史诗卡权重 +5 |
| 通过标准 | 后续升级中稀有/史诗卡出现频率明显提升；达到 2 次后不再出现 |
| 失败标准 | 权重未变更 / 超限后仍可抽取 |

### 2.16 赏金模块 (bounty-module)

| 编号 | TC-SK-016 |
|---|---|
| 前置条件 | `bountyCount < 3` |
| 执行步骤 | 1. 选择"赏金模块"卡片；2. 击杀敌人 |
| 预期结果 | `bountyCount += 1`；击杀得分倍率 = `1 + bountyCount * 0.25` |
| 通过标准 | 击杀得分 = 基础值 × 倍率（1.25 / 1.5 / 1.75）；达到 3 次后不再出现 |
| 失败标准 | 得分倍率计算错误 / 超限后仍可抽取 |

---

## 三、伤害计算验证 (Damage Calculation Verification)

### 3.1 普通子弹基础伤害

| 编号 | TC-DM-001 |
|---|---|
| 前置条件 | `bulletDamageBonus = 0`；`fireLevel = 1` |
| 执行步骤 | 1. 射击命中普通敌人（HP=1） |
| 预期结果 | 子弹伤害 = 1；普通敌人 HP 从 1 降至 0，被击杀 |
| 通过标准 | 敌人被一击消灭；击杀经验 5；击杀得分 20 |
| 失败标准 | 敌人未被击杀 / 伤害值不为 1 |

### 3.2 子弹强化叠加伤害

| 编号 | TC-DM-002 |
|---|---|
| 前置条件 | `bulletDamageBonus = 3`（子弹伤害 = 4） |
| 执行步骤 | 1. 射击命中精英敌人（HP=3） |
| 预期结果 | 一发子弹造成 4 点伤害，精英敌人被一击击杀 |
| 通过标准 | 精英敌人 HP 从 3 降至 -1；击杀经验 15；击杀得分 80 |
| 失败标准 | 精英敌人未被一击击杀 / 伤害计算不正确 |

### 3.3 导弹基础伤害与穿透

| 编号 | TC-DM-003 |
|---|---|
| 前置条件 | `hasExplosiveMissiles = false`；`missileCount > 0` |
| 执行步骤 | 1. 发射导弹；2. 导弹依次穿过 2 个普通敌人 |
| 预期结果 | 导弹伤害 = 2；穿透第一个敌人后继续飞行；两个敌人均受 2 点伤害 |
| 通过标准 | 第一个敌人 HP 从 1 降至 -1（击杀）；第二个敌人 HP 从 1 降至 -1（击杀）；导弹未消失 |
| 失败标准 | 导弹命中第一个敌人后消失 / 伤害不为 2 |

### 3.4 爆裂弹头范围伤害

| 编号 | TC-DM-004 |
|---|---|
| 前置条件 | `hasExplosiveMissiles = true`；3 个敌人聚集在爆炸半径 74px 内 |
| 执行步骤 | 1. 发射导弹命中中间敌人 |
| 预期结果 | 导弹对命中目标造成 2 点伤害；爆炸对范围内所有敌人造成额外伤害 |
| 通过标准 | 范围内敌人全部受到伤害；爆炸特效显示；范围外敌人不受影响 |
| 失败标准 | 范围伤害未生效 / 爆炸半径不正确 / 范围外敌人受伤 |

### 3.5 多子弹散射伤害

| 编号 | TC-DM-005 |
|---|---|
| 前置条件 | `fireLevel = 3`；`bulletDamageBonus = 1`（每颗子弹伤害 = 2） |
| 执行步骤 | 1. 射击；2. 3 颗子弹分别命中不同目标 |
| 预期结果 | 每颗子弹独立造成 2 点伤害 |
| 通过标准 | 3 个目标各受 2 点伤害；总伤害输出 = 6 |
| 失败标准 | 子弹伤害叠加错误 / 部分子弹未造成伤害 |

### 3.6 护甲伤害抵扣

| 编号 | TC-DM-006 |
|---|---|
| 前置条件 | `life = 3`；`armor = 2` |
| 执行步骤 | 1. 角色被敌人碰撞（伤害 = 1） |
| 预期结果 | 护甲先扣减：`armor` 从 2 降至 1；`life` 保持 3 |
| 通过标准 | 受击后 `armor = 1`，`life = 3`；进入无敌状态 |
| 失败标准 | 生命先扣减 / 护甲和生命同时扣减 |

### 3.7 护甲耗尽后生命扣减

| 编号 | TC-DM-007 |
|---|---|
| 前置条件 | `life = 3`；`armor = 1` |
| 执行步骤 | 1. 角色被敌人碰撞（伤害 = 1）；2. 再次被碰撞（无敌时间已过） |
| 预期结果 | 第一次：`armor` 从 1 降至 0；第二次：`life` 从 3 降至 2 |
| 通过标准 | 护甲优先消耗完毕后才开始扣生命 |
| 失败标准 | 护甲为 0 时仍先扣护甲 / 伤害穿透护甲 |

### 3.8 受击无敌时间

| 编号 | TC-DM-008 |
|---|---|
| 前置条件 | `invulnerableMs = 300`；角色刚被碰撞 |
| 执行步骤 | 1. 在 300ms 内再次被碰撞 |
| 预期结果 | 无敌期间不受伤 |
| 通过标准 | `life` 和 `armor` 不变；角色闪烁效果（alpha 0.55 交替） |
| 失败标准 | 无敌期内再次受伤 / 无敌时间不正确 |

### 3.9 Boss 导弹伤害

| 编号 | TC-DM-009 |
|---|---|
| 前置条件 | Boss 已生成并发射导弹 |
| 执行步骤 | 1. Boss 导弹命中角色 |
| 预期结果 | 造成 1 点伤害（先扣护甲再扣生命） |
| 通过标准 | 伤害值 = 1；触发无敌时间 |
| 失败标准 | 伤害值不正确 / 无敌时间未触发 |

### 3.10 障碍物碰撞伤害

| 编号 | TC-DM-010 |
|---|---|
| 前置条件 | 角色在地面上，障碍物接近 |
| 执行步骤 | 1. 角色与障碍物碰撞 |
| 预期结果 | 造成 1 点伤害；触发 `hitStopMs = 1000ms`（游戏短暂停顿） |
| 通过标准 | 伤害 = 1；游戏画面停顿约 1 秒；障碍物不可被摧毁 |
| 失败标准 | 无停顿效果 / 障碍物被摧毁 / 伤害值不正确 |

### 3.11 经验增幅叠加计算

| 编号 | TC-DM-011 |
|---|---|
| 前置条件 | `expBoostCount = 2`（倍率 = 1.4） |
| 执行步骤 | 1. 击杀普通敌人（基础经验 = 5） |
| 预期结果 | 获得经验 = `round(5 * 1.4)` = 7 |
| 通过标准 | `hero.exp` 增加 7 |
| 失败标准 | 经验值不正确 / 小数未取整 |

### 3.12 赏金模块叠加计算

| 编号 | TC-DM-012 |
|---|---|
| 前置条件 | `bountyCount = 2`（倍率 = 1.5） |
| 执行步骤 | 1. 击杀精英敌人（基础得分 = 80） |
| 预期结果 | 击杀得分 = `round(80 * 1.5)` = 120 |
| 通过标准 | `killScore` 增加 120 |
| 失败标准 | 得分计算不正确 / 小数未取整 |

### 3.13 最终得分公式

| 编号 | TC-DM-013 |
|---|---|
| 前置条件 | 游戏结束；距离 = 1500m；`killScore` = 500；等级 = 5 |
| 执行步骤 | 1. 查看结算界面得分 |
| 预期结果 | 最终得分 = `floor(1500) * 10 + round(500) + 5 * 100` = 15000 + 500 + 500 = 16000 |
| 通过标准 | 结算界面显示 16000 分 |
| 失败标准 | 得分计算公式错误 / 各分项未正确累加 |

---

## 四、碰撞检测测试 (Collision Detection Testing)

### 4.1 角色与障碍物碰撞

| 编号 | TC-CO-001 |
|---|---|
| 前置条件 | 角色在地面上；障碍物从右侧接近 |
| 执行步骤 | 1. 不跳跃，让障碍物与角色重叠 |
| 预期结果 | 矩形碰撞检测触发；角色受伤 |
| 通过标准 | `rectsOverlap` 返回 `true`；伤害生效；`hitStopMs = 1000` |
| 失败标准 | 碰撞未触发 / 碰撞区域偏移 / 角色穿过障碍 |

### 4.2 角色跳跃越过障碍

| 编号 | TC-CO-002 |
|---|---|
| 前置条件 | 角色在地面上；障碍物从右侧接近 |
| 执行步骤 | 1. 适时跳跃；2. 障碍物从角色下方通过 |
| 预期结果 | 无碰撞；角色不受伤害 |
| 通过标准 | 跳跃期间角色 Y 坐标高于障碍物顶部，矩形不重叠 |
| 失败标准 | 跳跃期间仍被碰撞 / 碰撞框过大 |

### 4.2.1 跳跃帧率无关（高刷屏越过移动敌人）

| 编号 | TC-CO-002F |
|---|---|
| 前置条件 | 角色在地面上；以不同刷新率（60Hz / 144Hz）推进主循环 |
| 执行步骤 | 1. 起跳；2. 分别以 `deltaMs=16.67`（大步）与 `6.94`（小步）积分至落地 |
| 预期结果 | 滞空时间（毫秒）与弧高与帧率无关，敌人有足够时间从下方滑过 |
| 通过标准 | 60Hz 与 144Hz 的滞空毫秒差异 ≤15%、弧高差异 ≤12%；`stepJump` 按 `deltaMs` 缩放 |
| 失败标准 | 退回按帧积分（无 `deltaMs` 缩放）→ 高刷屏滞空仅约 0.42 倍 → 跳不过移动敌人 |
| 自动化 | `tests/game-logic.test.ts` → `describe("stepJump（跳跃帧率无关…）")` |

### 4.3 角色飞行无视地面障碍

| 编号 | TC-CO-003 |
|---|---|
| 前置条件 | 角色处于飞行状态（Y=180） |
| 执行步骤 | 1. 障碍物从角色下方通过 |
| 预期结果 | 无碰撞 |
| 通过标准 | 飞行高度远高于障碍物，矩形不重叠 |
| 失败标准 | 飞行期间被地面障碍碰撞 |

### 4.4 角色与普通敌人碰撞

| 编号 | TC-CO-004 |
|---|---|
| 前置条件 | 角色在地面上；普通敌人接近 |
| 执行步骤 | 1. 让敌人与角色重叠 |
| 预期结果 | 碰撞触发；角色受 1 点伤害；敌人不因碰撞消失 |
| 通过标准 | 角色受伤；敌人继续存在（除非被子弹击杀） |
| 失败标准 | 碰撞后敌人消失 / 角色未受伤 |

### 4.5 子弹与敌人碰撞

| 编号 | TC-CO-005 |
|---|---|
| 前置条件 | 子弹向右飞行；敌人在前方 |
| 执行步骤 | 1. 子弹与敌人重叠 |
| 预期结果 | 碰撞触发；敌人 HP 扣减；子弹消失（非穿透） |
| 通过标准 | 敌人 HP 减少；子弹 `active = false`；碰撞框使用缩小后的 hitbox（entity +6, +4, -12, -8） |
| 失败标准 | 子弹穿过敌人 / 伤害未生效 / hitbox 不正确 |

### 4.6 导弹穿透碰撞

| 编号 | TC-CO-006 |
|---|---|
| 前置条件 | 导弹向右飞行；2 个敌人在导弹路径上 |
| 执行步骤 | 1. 导弹命中第一个敌人；2. 继续飞行命中第二个敌人 |
| 预期结果 | 两个敌人均受伤害；导弹不消失 |
| 通过标准 | 第一个敌人 HP 减少；第二个敌人 HP 减少；导弹 `active` 仍为 `true` |
| 失败标准 | 导弹命中第一个敌人后消失 |

### 4.7 子弹与障碍物无碰撞

| 编号 | TC-CO-007 |
|---|---|
| 前置条件 | 子弹向右飞行；障碍物在前方 |
| 执行步骤 | 1. 子弹与障碍物重叠 |
| 预期结果 | 无碰撞；子弹穿过障碍物继续飞行 |
| 通过标准 | 子弹 `active` 保持 `true`；障碍物 HP 不变（Infinity） |
| 失败标准 | 子弹被障碍物阻挡 / 障碍物 HP 变化 |

### 4.8 Boss 导弹与角色碰撞

| 编号 | TC-CO-008 |
|---|---|
| 前置条件 | Boss 已发射导弹；导弹向左飞行 |
| 执行步骤 | 1. Boss 导弹与角色重叠 |
| 预期结果 | 碰撞触发；角色受 1 点伤害 |
| 通过标准 | 角色受伤；Boss 导弹消失 |
| 失败标准 | Boss 导弹穿过角色 / 伤害值不正确 |

### 4.9 碰撞框精度 — 敌人 hitbox 缩小

| 编号 | TC-CO-009 |
|---|---|
| 前置条件 | 子弹接近敌人边缘 |
| 执行步骤 | 1. 子弹仅与敌人视觉精灵边缘重叠，但未进入缩小后的 hitbox |
| 预期结果 | 无碰撞判定 |
| 通过标准 | hitbox 比视觉精灵缩小（x+6, y+4, width-12, height-8），边缘附近不误判 |
| 失败标准 | 视觉边缘即触发碰撞 / hitbox 缩小过多导致子弹穿过 |

### 4.10 连续碰撞防叠加

| 编号 | TC-CO-010 |
|---|---|
| 前置条件 | 角色无敌时间刚结束；2 个敌人同时接触角色 |
| 执行步骤 | 1. 两敌人同时与角色碰撞 |
| 预期结果 | 只触发一次伤害（无敌时间生效） |
| 通过标准 | 第一次碰撞后进入无敌；第二次碰撞在无敌期内不生效 |
| 失败标准 | 同一帧内多次受伤 / 伤害叠加 |

---

## 五、游戏玩法验证 (Gameplay Validation)

### 5.1 完整单局流程

| 编号 | TC-GP-001 |
|---|---|
| 前置条件 | 游戏加载完成 |
| 执行步骤 | 1. 进入菜单 → 2. 开始游戏 → 3. 跑酷/射击/升级 → 4. 死亡 → 5. 结算 |
| 预期结果 | 完整流程无阻断；各场景切换正确 |
| 通过标准 | MenuScene → GameScene → LevelUpScene(可选) → GameOverScene；结算数据正确 |
| 失败标准 | 场景切换卡死 / 数据丢失 / 流程中断 |

### 5.2 自动前进与速度递增

| 编号 | TC-GP-002 |
|---|---|
| 前置条件 | 游戏开始 |
| 执行步骤 | 1. 观察跑酷速度随距离变化 |
| 预期结果 | 初始速度 2.5；每 800m 提升 0.3；最高 8.0 |
| 通过标准 | `gameSpeed = clamp(2.5 + difficultyTier * 0.3, 2.5, 8.0)`；距离/800m 对应速度档位正确 |
| 失败标准 | 速度未递增 / 速度超过 8.0 / 递增间隔不正确 |

### 5.3 敌人生成规则

| 编号 | TC-GP-003 |
|---|---|
| 前置条件 | 游戏进行中 |
| 执行步骤 | 1. 观察不同距离段的敌人类型生成 |
| 预期结果 | 0-300m：仅障碍+普通敌人；300-1200m：加入精英敌人；1200m+：加入 Boss |
| 通过标准 | 各阶段敌人类型符合 PRD 规则；生成间隔 0.8s-1.8s；最小安全间距 650px |
| 失败标准 | 前期出现 Boss / 精英过早出现 / 生成间距过小 |

### 5.4 Boss 生成间隔

| 编号 | TC-GP-004 |
|---|---|
| 前置条件 | 第一个 Boss 已被击杀 |
| 执行步骤 | 1. 继续跑酷；2. 观察下一个 Boss 出现时机 |
| 预期结果 | 下一个 Boss 在距离 +1200m 或时间 +90s 后出现 |
| 通过标准 | Boss 出现间距符合 `nextBossDistance += 1200` 或 `timeSinceBossMs >= 90000` |
| 失败标准 | Boss 出现过频 / 间距不正确 / 同时出现两个 Boss |

### 5.5 升级选卡流程

| 编号 | TC-GP-005 |
|---|---|
| 前置条件 | 经验达到升级阈值 |
| 执行步骤 | 1. 游戏暂停；2. 弹出 3 张卡片；3. 选择 1 张 |
| 预期结果 | 游戏暂停进入 LEVELUP 状态；显示 3 张不重复卡片；选择后立即生效并恢复游戏 |
| 通过标准 | 3 张卡片不完全重复；已达上限的卡片不出现在候选池；选中后属性立即变更 |
| 失败标准 | 卡片重复 / 已达上限卡片出现 / 选择后属性未变更 / 游戏未恢复 |

### 5.6 连续升级（多次升级积压）

| 编号 | TC-GP-006 |
|---|---|
| 前置条件 | 一次获得大量经验，触发 2 次以上升级 |
| 执行步骤 | 1. `pendingLevelUps >= 2`；2. 选择第一张卡；3. 立即弹出新的 3 张卡 |
| 预期结果 | 连续选卡；每次选卡后刷新候选池；全部选完后恢复游戏 |
| 通过标准 | 选卡界面刷新而非关闭重开；`pendingLevelUps` 逐次递减至 0；提示剩余升级次数 |
| 失败标准 | 只弹出一次选卡 / 积压升级丢失 / 界面异常 |

### 5.7 卡片稀有度权重

| 编号 | TC-GP-007 |
|---|---|
| 前置条件 | `luckyCount = 0` |
| 执行步骤 | 1. 统计 100 次升级的卡片稀有度分布 |
| 预期结果 | 普通 ~60%、稀有 ~30%、史诗 ~10% |
| 通过标准 | 分布比例接近权重设定；`luckyCount > 0` 时稀有/史诗比例上升 |
| 失败标准 | 分布严重偏离 / 史诗卡从不出现 |

### 5.8 游戏结束条件

| 编号 | TC-GP-008 |
|---|---|
| 前置条件 | `life <= 0` |
| 执行步骤 | 1. 角色生命值归零 |
| 预期结果 | 游戏进入 GAMEOVER 状态；BGM 停止；播放 `gameover.wav`；弹出结算界面 |
| 通过标准 | `state = GAMEOVER`；结算数据正确（得分、距离、击杀、Boss 击杀、等级、构筑摘要） |
| 失败标准 | 游戏未结束 / 结算数据错误 / 无结算界面 |

### 5.9 重新开始状态重置

| 编号 | TC-GP-009 |
|---|---|
| 前置条件 | GameOverScene 显示 |
| 执行步骤 | 1. 点击"再来一局" |
| 预期结果 | 所有游戏状态完全重置 |
| 通过标准 | `hero` 恢复初始值；所有实体/子弹/爆炸清除；距离/分数/击杀归零；速度恢复初始 |
| 失败标准 | 残留上一局数据 / 实体未清除 / 属性未重置 |

### 5.10 障碍物不可摧毁

| 编号 | TC-GP-010 |
|---|---|
| 前置条件 | 障碍物在场景中 |
| 执行步骤 | 1. 射击命中障碍物；2. 发射导弹命中障碍物 |
| 预期结果 | 障碍物不受任何伤害；子弹/导弹穿过或消失 |
| 通过标准 | 障碍物 HP = Infinity 不变；碰撞检测跳过障碍物类型 |
| 失败标准 | 障碍物被摧毁 / 障碍物 HP 变化 |

### 5.11 Boss 行为验证

| 编号 | TC-GP-011 |
|---|---|
| 前置条件 | Boss 已生成（HP=8） |
| 执行步骤 | 1. 观察 Boss 行为 |
| 预期结果 | Boss 向左移动（speedScale=0.82）；每 1.2s 发射一枚导弹；HP 条显示在头顶 |
| 通过标准 | Boss 移动速度比普通敌人慢；发射间隔 1200ms；HP 条实时更新 |
| 失败标准 | Boss 不发射导弹 / HP 条不更新 / 移动速度异常 |

### 5.12 Boss 击杀反馈

| 编号 | TC-GP-012 |
|---|---|
| 前置条件 | Boss HP 即将归零 |
| 执行步骤 | 1. 击杀 Boss |
| 预期结果 | 大爆炸特效；`hitStopMs = 320ms`；公告"Boss Down"；经验 30；得分 200 |
| 通过标准 | 爆炸半径 96px；短暂停顿；公告显示 1600ms；击杀数+1，Boss 击杀数+1 |
| 失败标准 | 无停顿 / 无公告 / 奖励不正确 |

---

## 六、叙事与任务流程测试 (Narrative & Mission Flow Testing)

### 6.1 场景启动顺序

| 编号 | TC-NR-001 |
|---|---|
| 前置条件 | 游戏首次加载 |
| 执行步骤 | 1. BootScene 加载资源 → 2. MenuScene 显示 → 3. 开始游戏 → GameScene |
| 预期结果 | 场景按 BootScene → MenuScene → GameScene 顺序切换 |
| 通过标准 | 资源加载完成后再显示菜单；无黑屏或闪烁 |
| 失败标准 | 跳过 BootScene / 资源未加载完毕就显示菜单 |

### 6.2 资源加载完整性

| 编号 | TC-NR-002 |
|---|---|
| 前置条件 | BootScene 的 `preload` 执行 |
| 执行步骤 | 1. 检查所有图片和音频资源是否成功加载 |
| 预期结果 | 20 张图片 + 8 个音频全部加载成功 |
| 通过标准 | 无加载错误；所有 sprite key 可正常使用 |
| 失败标准 | 缺失资源 / key 冲突 / 加载失败 |

### 6.3 公告系统 — Boss 出现

| 编号 | TC-NR-003 |
|---|---|
| 前置条件 | Boss 生成条件满足 |
| 执行步骤 | 1. Boss 生成 |
| 预期结果 | 显示"Boss Incoming"公告（1600ms）；播放 `boss_spawn.wav` |
| 通过标准 | 公告文字居中显示；1.6 秒后自动消失；音效播放 |
| 失败标准 | 无公告 / 公告不消失 / 无音效 |

### 6.4 公告系统 — 升级选择

| 编号 | TC-NR-004 |
|---|---|
| 前置条件 | 选择升级卡片 |
| 执行步骤 | 1. 选择任意升级卡 |
| 预期结果 | 显示卡片名称公告（飞行模块显示"Flight Online"）；1600ms 后消失 |
| 通过标准 | 公告文字正确；自动消失 |
| 失败标准 | 公告文字错误 / 不消失 |

### 6.5 难度阶段过渡

| 编号 | TC-NR-005 |
|---|---|
| 前置条件 | 距离从 0 增长至 2400m+ |
| 执行步骤 | 1. 观察各阶段难度变化 |
| 预期结果 | 0-800m：基础难度；800-2400m：精英+Boss 介入；2400m+：极限生存 |
| 通过标准 | 速度递增；敌人生成频率提高；精英/Boss 按规则出现 |
| 失败标准 | 难度无变化 / 阶段过渡不明显 |

### 6.6 暂停/恢复叙事连贯性

| 编号 | TC-NR-006 |
|---|---|
| 前置条件 | 游戏进行中，Boss 即将出现 |
| 执行步骤 | 1. 暂停游戏；2. 恢复游戏 |
| 预期结果 | 暂停/恢复后游戏状态完全一致；Boss 生成逻辑不受暂停影响 |
| 通过标准 | 恢复后敌人位置/速度/HP 不变；计时器正确续接 |
| 失败标准 | 暂停导致状态丢失 / 恢复后敌人跳变 |

### 6.7 游戏结束叙事完整性

| 编号 | TC-NR-007 |
|---|---|
| 前置条件 | 角色死亡 |
| 执行步骤 | 1. 生命归零；2. 查看结算界面 |
| 预期结果 | BGM 停止 → `gameover.wav` 播放 → 结算界面显示完整数据 |
| 通过标准 | 结算包含：最终得分、距离、击杀总数、Boss 击杀数、等级、构筑摘要 |
| 失败标准 | 结算数据缺失 / BGM 未停止 / 无音效 |

### 6.8 构筑摘要展示

| 编号 | TC-NR-008 |
|---|---|
| 前置条件 | 一局游戏中选择了多个升级 |
| 执行步骤 | 1. 游戏结束；2. 查看结算界面的"本局构筑" |
| 预期结果 | 显示所有已选升级名称，以" / "分隔 |
| 通过标准 | 构筑摘要 = `upgradeHistory.join(" / ")`；未选升级时显示"未成型构筑" |
| 失败标准 | 构筑摘要缺失 / 顺序错误 / 空构筑显示异常 |

---

## 七、性能测试 (Performance Testing)

### 7.1 目标帧率稳定性

| 编号 | TC-PF-001 |
|---|---|
| 前置条件 | 推荐配置硬件（中端 PC/笔记本） |
| 执行步骤 | 1. 运行游戏 10 分钟；2. 使用浏览器 DevTools Performance 面板记录帧率 |
| 预期结果 | 帧率稳定在 60 FPS；无持续掉帧 |
| 通过标准 | 95% 以上帧时间 ≤ 16.67ms；最低帧率不低于 50 FPS |
| 失败标准 | 平均帧率低于 55 FPS / 频繁掉帧 |

### 7.2 大量实体渲染性能

| 编号 | TC-PF-002 |
|---|---|
| 前置条件 | 场景中同时存在 20+ 实体、10+ 子弹、5+ 爆炸特效 |
| 执行步骤 | 1. 在高密度战斗场景下观察帧率 |
| 预期结果 | 帧率无明显下降 |
| 通过标准 | 实体数量峰值时帧率不低于 50 FPS |
| 失败标准 | 实体增多时帧率骤降 / 卡顿明显 |

### 7.3 内存使用监控

| 编号 | TC-PF-003 |
|---|---|
| 前置条件 | 游戏运行中 |
| 执行步骤 | 1. 使用 DevTools Memory 面板监控内存；2. 连续游玩 30 分钟 |
| 预期结果 | 内存使用稳定，无明显增长趋势 |
| 通过标准 | 30 分钟内内存增长 < 50MB；无持续上升趋势 |
| 失败标准 | 内存持续增长 / 30 分钟增长超过 100MB |

### 7.4 资源加载时间

| 编号 | TC-PF-004 |
|---|---|
| 前置条件 | 清除浏览器缓存 |
| 执行步骤 | 1. 首次加载游戏；2. 测量从打开页面到 MenuScene 显示的时间 |
| 预期结果 | 加载时间 < 3 秒（普通网络） |
| 通过标准 | 资源下载 + 解码总时间 < 3s；无长时间白屏 |
| 失败标准 | 加载时间超过 5 秒 / 白屏超过 2 秒 |

### 7.5 实体清理效率

| 编号 | TC-PF-005 |
|---|---|
| 前置条件 | 大量实体已离开屏幕标记为 `active=false` |
| 执行步骤 | 1. 观察 `cleanup()` 执行后的实体数量 |
| 预期结果 | 离屏实体及时销毁；对象池不无限增长 |
| 通过标准 | `entities`/`playerProjectiles`/`enemyProjectiles`/`explosions` 数组长度保持合理范围 |
| 失败标准 | 数组持续增长 / 未调用 `destroy()` 导致内存泄漏 |

### 7.6 最低配置测试

| 编号 | TC-PF-006 |
|---|---|
| 前置条件 | 低端硬件（集成显卡、4GB 内存、2 核 CPU） |
| 执行步骤 | 1. 运行游戏完整一局 |
| 预期结果 | 游戏可玩；帧率不低于 30 FPS |
| 通过标准 | 核心玩法可正常体验；无崩溃 |
| 失败标准 | 帧率低于 20 FPS / 频繁卡顿 / 崩溃 |

### 7.7 高端配置测试

| 编号 | TC-PF-007 |
|---|---|
| 前置条件 | 高端硬件（独立显卡、16GB+ 内存、8 核 CPU） |
| 执行步骤 | 1. 运行游戏；2. 观察是否有异常高速/跳帧 |
| 预期结果 | 帧率稳定 60 FPS（受 Phaser 帧率限制）；无异常加速 |
| 通过标准 | `deltaMs` 被 `Math.min(delta, 50)` 限制，无时间跳跃 |
| 失败标准 | 游戏速度异常 / 物理计算跳帧 |

### 7.8 移动端性能

| 编号 | TC-PF-008 |
|---|---|
| 前置条件 | 中端移动设备（如 iPhone 13 / 小米 12） |
| 执行步骤 | 1. 在移动浏览器中运行游戏 10 分钟 |
| 预期结果 | 帧率稳定在 30+ FPS；触控响应流畅 |
| 通过标准 | 核心玩法可体验；电池消耗合理（< 15%/10min） |
| 失败标准 | 帧率低于 20 FPS / 触控延迟 > 100ms / 过热 |

---

## 八、稳定性测试 (Stability Testing)

### 8.1 长时间运行 — 无崩溃

| 编号 | TC-ST-001 |
|---|---|
| 前置条件 | 游戏正常运行 |
| 执行步骤 | 1. 连续游玩 60 分钟（自动/半自动） |
| 预期结果 | 无崩溃、无冻结、无内存溢出 |
| 通过标准 | 60 分钟内游戏持续运行；可正常暂停/恢复/重新开始 |
| 失败标准 | 崩溃 / 冻结超过 3 秒 / 内存溢出 |

### 8.2 快速连续输入压力测试

| 编号 | TC-ST-002 |
|---|---|
| 前置条件 | 游戏处于 PLAYING 状态 |
| 执行步骤 | 1. 以最快速度连续按 Space + K + L（每秒 10+ 次组合输入） |
| 预期结果 | 游戏不崩溃；输入按冷却规则正确处理 |
| 通过标准 | 无异常；冷却机制正常工作；无子弹/导弹数量异常 |
| 失败标准 | 崩溃 / 子弹数量爆炸 / 输入队列溢出 |

### 8.3 多次暂停/恢复循环

| 编号 | TC-ST-003 |
|---|---|
| 前置条件 | 游戏运行中 |
| 执行步骤 | 1. 快速交替按 P 键暂停/恢复 50 次 |
| 预期结果 | 游戏状态正确切换；无场景叠加或泄漏 |
| 通过标准 | 最终状态与操作一致；PauseScene 无重复叠加；BGM 状态正确 |
| 失败标准 | 场景叠加 / BGM 异常 / 状态错乱 |

### 8.4 多次重新开始循环

| 编号 | TC-ST-004 |
|---|---|
| 前置条件 | GameOverScene 显示 |
| 执行步骤 | 1. 连续执行"重新开始 → 死亡 → 重新开始" 20 次 |
| 预期结果 | 每次重开状态完全重置；无实体残留 |
| 通过标准 | 每局初始状态一致；内存无持续增长；无场景叠加 |
| 失败标准 | 实体残留 / 内存增长 / 场景叠加 / 崩溃 |

### 8.5 升级选卡边界 — 所有卡片达上限

| 编号 | TC-ST-005 |
|---|---|
| 前置条件 | 所有有上限的升级均已达到上限（仅剩 `missile-supply` 和 `flight-module` 无上限） |
| 执行步骤 | 1. 再次升级；2. 观察候选池 |
| 预期结果 | 候选池仅包含未达上限的卡片；卡片数量可能少于 3 张 |
| 通过标准 | `eligible` 过滤后返回可用卡片；`eligible.length <= 3` 时直接返回全部 |
| 失败标准 | 无卡片可选导致崩溃 / 已达上限卡片仍出现 |

### 8.6 极端距离测试

| 编号 | TC-ST-006 |
|---|---|
| 前置条件 | 游戏运行中 |
| 执行步骤 | 1. 通过调试手段将距离设为极大值（如 999999m） |
| 预期结果 | 游戏继续运行；速度达到上限 8.0 后不再增加 |
| 通过标准 | `gameSpeed` 被 `clamp` 限制在 8.0；得分计算不溢出 |
| 失败标准 | 速度无限增长 / 得分溢出 / 崩溃 |

### 8.7 极端等级测试

| 编号 | TC-ST-007 |
|---|---|
| 前置条件 | 通过调试将等级设为极大值（如 999） |
| 执行步骤 | 1. 观察升级经验需求 `nextLevelExp = level * 10` |
| 预期结果 | 经验需求随等级线性增长；数值不溢出 |
| 通过标准 | `nextLevelExp` 计算正确；HUD 显示正常 |
| 失败标准 | 数值溢出 / HUD 显示异常 / 崩溃 |

### 8.8 同时大量爆炸特效

| 编号 | TC-ST-008 |
|---|---|
| 前置条件 | 场景中有 10+ 敌人密集排列 |
| 执行步骤 | 1. 使用爆裂弹头导弹命中密集敌群 |
| 预期结果 | 所有敌人被击杀；爆炸特效正常显示和消退 |
| 通过标准 | 爆炸特效 240ms 后消失；`alpha` 渐变正常；`cleanup()` 正确清理 |
| 失败标准 | 爆炸特效不消失 / 帧率骤降 / 崩溃 |

### 8.9 飞行状态结束落在障碍上

| 编号 | TC-ST-009 |
|---|---|
| 前置条件 | 角色飞行中；飞行即将结束；正下方有障碍物 |
| 执行步骤 | 1. 飞行倒计时结束；2. 角色落回地面 |
| 预期结果 | 角色回到地面默认 Y 坐标；若落点在障碍上，应平滑回落至安全位置 |
| 通过标准 | 角色不卡入障碍；落地后正常受碰撞规则约束 |
| 失败标准 | 角色卡入障碍 / 穿入地面 / 无法操作 |

### 8.10 浏览器标签页切换

| 编号 | TC-ST-010 |
|---|---|
| 前置条件 | 游戏运行中 |
| 执行步骤 | 1. 切换到其他标签页；2. 等待 30 秒；3. 切回游戏标签页 |
| 预期结果 | Phaser 自动暂停；切回后游戏可正常继续 |
| 通过标准 | 切回后无时间跳跃（`deltaMs` 被 `Math.min(delta, 50)` 限制）；游戏状态一致 |
| 失败标准 | 切回后游戏加速 / 崩溃 / 状态异常 |

### 8.11 窗口缩放适配

| 编号 | TC-ST-011 |
|---|---|
| 前置条件 | 游戏运行中 |
| 执行步骤 | 1. 调整浏览器窗口大小（缩小/放大/全屏） |
| 预期结果 | 游戏画面按 `Phaser.Scale.FIT` + `CENTER_BOTH` 正确适配 |
| 通过标准 | 画面比例不变；无拉伸变形；HUD 可见 |
| 失败标准 | 画面变形 / HUD 超出可视区域 / 点击区域偏移 |

### 8.12 音频上下文恢复

| 编号 | TC-ST-012 |
|---|---|
| 前置条件 | 浏览器自动暂停了 AudioContext（常见于首次交互前） |
| 执行步骤 | 1. 页面加载后不点击直接等待；2. 首次点击"开始游戏" |
| 预期结果 | AudioContext 在用户交互后恢复；BGM 正常播放 |
| 通过标准 | 首次交互后音效正常；无静音问题 |
| 失败标准 | 持续无声音 / AudioContext 未恢复 |

---

## 附录：测试环境矩阵

| 维度 | 配置 |
|---|---|
| 浏览器 | Chrome 120+ / Firefox 120+ / Safari 17+ / Edge 120+ |
| 操作系统 | Windows 10/11 / macOS 14+ / Android 13+ / iOS 17+ |
| 分辨率 | 1024×560（基准）/ 1920×1080 / 2560×1440 / 移动端自适应 |
| 输入设备 | 键盘 / 鼠标 / 触屏 |
| 网络条件 | 本地 / 3G 模拟 / 离线（Service Worker 缓存后） |

---

## 附录：关键数值速查

| 项目 | 值 | 来源 |
|---|---|---|
| 基础分辨率 | 1024×560 | `CONFIG.BASE_WIDTH/HEIGHT` |
| 初始速度 | 2.5 | `CONFIG.INITIAL_SPEED` |
| 最高速度 | 8.0 | `CONFIG.MAX_SPEED` |
| 速度递增间隔 | 800m | `CONFIG.SPEED_MILESTONE_INTERVAL` |
| 每档速度增量 | +0.3 | `CONFIG.SPEED_STEP` |
| 跳跃初速度 | -12 | `CONFIG.JUMP_VELOCITY` |
| 重力 | 0.4 | `CONFIG.GRAVITY` |
| 射击冷却 | 200ms | `CONFIG.SHOOT_COOLDOWN_MS` |
| 导弹冷却 | 1500ms | `CONFIG.MISSILE_COOLDOWN_MS` |
| 受击无敌 | 300ms | `CONFIG.INVULNERABLE_MS` |
| 飞行持续 | 10000ms | `CONFIG.DEFAULT_FLIGHT_MS` |
| 飞行上限 | 19000ms | `CONFIG.MAX_FLIGHT_MS` |
| 普通敌人 HP | 1 | `EncounterDirector` |
| 精英敌人 HP | 3 | `EncounterDirector` |
| Boss HP | 8 | `EncounterDirector` |
| 子弹速度 | 520px/s | `GameScene` |
| Boss 导弹速度 | 480px/s | `GameScene` |
| 爆炸半径 | 74px | `GameScene` |
| Boss 爆炸半径 | 96px | `GameScene` |
| 升级经验公式 | `level × 10` | `GameScene.heroGainExp` |
